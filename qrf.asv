function q = qrf(alpha,mu) % quantile function

    n = size(mu,1);
    ret = zerps(1,n);

    for i = 1:n

        if(alpha(1,i) < lambda(1,i))

            ret(1,i) = 0;
        else

            ret(1,i) = 2.*mu.*sqrt((-log(1-alpha))/pi);
        end
    end

    end 